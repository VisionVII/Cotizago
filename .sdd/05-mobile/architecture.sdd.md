# Mobile Architecture

Expo + React Native + TypeScript.

Navigation: Expo Router.

State:
- Zustand para estado local/UI.
- TanStack Query para server state.
- SQLite para dados operacionais offline.

Formulários:
React Hook Form + Zod.

A camada de dados deve impedir SQL espalhado pelos componentes.
